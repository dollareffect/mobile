import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    alignItems: 'center',
    justifyContent: 'center',
  },
  facebookButton: {
    marginLeft: 0,
    marginRight: 0,
    height: 50,
  },
});
